<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:id="@+id/deviceData"
    tools:context="fi.hiit.whatisstoredinamobiledevice.ui.activities.DeviceData">

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:enabled="false"
        android:onClick="sendCollectedDataToServer"
        android:text="@string/device_data_send_data_button"
        android:id="@+id/device_data_send_data_button" />

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:paddingStart="10dp"
        android:paddingLeft="10dp"
        android:paddingEnd="10dp"
        android:paddingRight="10dp"
        android:scrollbars = "vertical"
        android:id="@+id/device_data_text"
        android:text="@string/loading_text_during_collection"
        android:typeface="serif"
        android:textSize="20sp"
        />

</LinearLayout>